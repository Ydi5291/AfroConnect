import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afroconnect.app',
  appName: 'AfroConnect',
  webDir: 'dist/first-angular-project'
};

export default config;
